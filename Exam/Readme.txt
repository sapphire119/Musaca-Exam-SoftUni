Прехвърлих проекта от папката Apps и сега не може да се билдне,
Само трябва да се оправят dependency-тата.
Всичко иначе работа на 100%

---Deleted ConnectionConfig.cs you have to implement your specific settings. @"Server=...;Database=...;Trusted_Connection=True...;"
--The database was initialized through Powershell using "dotnet ef ..." commands.

There are some dependency issues that can easily be resolved,
everything else works as expeceted.